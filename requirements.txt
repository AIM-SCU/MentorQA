# Recommended Python version: 3.10

# Core model inference
# Install the PyTorch build appropriate for your OS/CUDA setup if this default
# PyPI build is not suitable: https://pytorch.org/get-started/locally/
torch>=2.1
transformers>=4.43,<5

# Video/audio preprocessing
openai-whisper>=20240930
yt-dlp>=2024.1.1

# RAG pipeline
langchain>=0.2,<0.4
langchain-community>=0.2,<0.4
chromadb>=0.5,<0.7
sentence-transformers>=3.0

# Spreadsheet input support (.xlsx and .xls files)
pandas>=2.0
openpyxl>=3.1
xlrd>=2.0
